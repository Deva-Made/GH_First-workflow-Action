name: Hello world

on:
  workflow_dispatch:
  inputs:
  name:
    Description: 'Name'
    type: string
    default: Test
  
jobs:
  demo:
    runs-on: ubuntu-latest

    steps:
      - name: Greetings
        run: echo "Hello, world"
